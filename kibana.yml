# Server configuration
server.host: "0.0.0.0"
server.port: 5601

# SSL configuration
server.ssl.enabled: true
server.ssl.certificate: "/usr/share/kibana/config/www.projectkkk.com.pem"
server.ssl.key: "/usr/share/kibana/config/www.projectkkk.com.pem"

# Elasticsearch configuration
elasticsearch.hosts: ["https://elasticsearch.www.projectkkk.com:9200"]
elasticsearch.username: "kibana_system"
elasticsearch.password: "Ccenter123456!"
elasticsearch.ssl.certificateAuthorities: ["/usr/share/kibana/config/www.projectkkk.com.pem"]
elasticsearch.ssl.verificationMode: "full"

# Security settings
xpack.security.enabled: true
xpack.encryptedSavedObjects.encryptionKey: "something_at_least_32_characters"

# CORS settings
server.cors.enabled: true
server.cors.allowCredentials: true
server.cors.allowOrigin: ["https://www.projectkkk.com"]