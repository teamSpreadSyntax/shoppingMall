# Server configuration
server.host: "0.0.0.0"
server.name: kibana
server.port: 5601

# SSL configuration
server.ssl.enabled: true
server.ssl.keystore.path: "/usr/share/elasticsearch/config/www.projectkkk.pkcs12"
server.ssl.keystore.password: "Ccenter123456!"


# Elasticsearch configuration
elasticsearch.hosts: ["https://elasticsearch.www.projectkkk.com:9200"]
elasticsearch.ssl.verificationMode: full
elasticsearch.ssl.certificateAuthorities: ["/usr/share/elasticsearch/config/www.projectkkk.pkcs12"]
elasticsearch.username: "kibana_system"
elasticsearch.password: "Ccenter123456!"

# Security settings
xpack.security.enabled: true
xpack.encryptedSavedObjects.encryptionKey: "something_at_least_32_characters"

# Additional settings for stability
elasticsearch.requestTimeout: 90000